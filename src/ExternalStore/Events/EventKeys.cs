namespace ExternalStore.Events
{
    public sealed class EventKeys
    {
        public sealed class Subscription
        {
            public const string Added = "subscription-added";
        }
    }
}
